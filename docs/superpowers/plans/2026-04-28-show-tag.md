# タグ表示機能 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** コミットに付けられた Git タグ（軽量・注釈付き両対応）をグラフ SVG とコミット詳細パネルに表示する。

**Architecture:** `Tag` テーブルを SQLite に追加し、同期時に pygit2 でタグを読み取ってキャッシュする。グラフ画面ではコミット円の右にタグ名をカンマ区切りで描画し、詳細パネルではフィールドとして表示する。

**Tech Stack:** Python 3.12 / pygit2 / SQLModel / FastAPI / Jinja2 / htmx

---

## ファイル構成

| 操作 | ファイル | 変更内容 |
|------|---------|---------|
| 修正 | `backend/models.py` | `Tag` モデル追加 |
| 修正 | `schema.hcl` | `tags` テーブル定義追加 |
| 修正 | `backend/repositories/git_repo.py` | `iter_tags` 関数追加 |
| 修正 | `backend/repositories/cache_repo.py` | Tag CRUD 関数追加、`purge_graph_data` でタグも削除 |
| 修正 | `backend/services/sync_service.py` | タグ同期を追加 |
| 修正 | `backend/routers/html.py` | グラフ・詳細エンドポイントでタグを渡す |
| 修正 | `backend/templates/graph.html` | タグラベル描画追加 |
| 修正 | `backend/templates/partials/detail.html` | タグフィールド追加 |
| 修正 | `tests/support/git_repo_fixture.py` | タグ付きリポジトリフィクスチャ追加 |
| 修正 | `tests/unit/test_git_repo.py` | `iter_tags` テスト追加 |
| 修正 | `tests/unit/test_cache_repo.py` | Tag CRUD テスト追加 |
| 修正 | `tests/unit/test_sync_service.py` | タグ同期テスト追加 |

---

### Task 1: Tag モデルと schema.hcl を追加する

**Files:**
- Modify: `backend/models.py`
- Modify: `schema.hcl`

- [ ] **Step 1: `Tag` モデルを `models.py` に追加する**

`backend/models.py` の末尾に追記する：

```python
class Tag(SQLModel, table=True):
    """タグ情報。"""

    __tablename__ = "tags"

    name: str = Field(primary_key=True)
    repo_id: str = Field(primary_key=True, foreign_key="repositories.id")
    commit_hash: str = Field(sa_column=Column(String, nullable=False))
```

- [ ] **Step 2: `schema.hcl` に `tags` テーブルを追記する**

`schema.hcl` の末尾に追記する：

```hcl
table "tags" {
  schema = schema.main
  column "name" {
    type = text
    null = false
  }
  column "repo_id" {
    type = text
    null = false
  }
  column "commit_hash" {
    type = text
    null = false
  }
  primary_key {
    columns = [column.name, column.repo_id]
  }
  foreign_key "tags_repo_fk" {
    columns     = [column.repo_id]
    ref_columns = [table.repositories.column.id]
  }
  foreign_key "tags_commit_fk" {
    columns     = [column.commit_hash]
    ref_columns = [table.commits.column.hash]
  }
}
```

- [ ] **Step 3: コミットする**

```bash
git add backend/models.py schema.hcl
git commit -m "feat: Tag モデルと schema.hcl を追加する"
```

---

### Task 2: テスト用フィクスチャとして `make_tagged_repo` を追加する

**Files:**
- Modify: `tests/support/git_repo_fixture.py`

- [ ] **Step 1: `make_tagged_repo` 関数を追加する**

`tests/support/git_repo_fixture.py` に追記する：

```python
def make_tagged_repo(path: Path) -> tuple[Path, str, str]:
    """軽量タグと注釈付きタグを持つリポジトリを作成する。

    Args:
        path: リポジトリのルートディレクトリ。

    Returns:
        (リポジトリパス, 軽量タグのコミットハッシュ, 注釈付きタグのコミットハッシュ)
    """
    path.mkdir(parents=True, exist_ok=True)
    repo = pygit2.init_repository(str(path), False)
    sig = pygit2.Signature("テスト", "t@example.com", int(time.time()), 0)

    (path / "a.txt").write_text("a\n", encoding="utf-8")
    repo.index.add("a.txt")
    repo.index.write()
    tree1 = repo.index.write_tree()
    oid1 = repo.create_commit("refs/heads/main", sig, sig, "first", tree1, [])

    (path / "b.txt").write_text("b\n", encoding="utf-8")
    repo.index.add("b.txt")
    repo.index.write()
    tree2 = repo.index.write_tree()
    oid2 = repo.create_commit("refs/heads/main", sig, sig, "second", tree2, [oid1])

    # 軽量タグ: 1 つ目のコミットに付ける
    repo.create_reference("refs/tags/v0.1", oid1, False)

    # 注釈付きタグ: 2 つ目のコミットに付ける
    repo.create_tag("v1.0", oid2, pygit2.GIT_OBJ_COMMIT, sig, "Release 1.0")

    return path, str(oid1), str(oid2)
```

- [ ] **Step 2: コミットする**

```bash
git add tests/support/git_repo_fixture.py
git commit -m "test: タグ付きリポジトリフィクスチャを追加する"
```

---

### Task 3: `iter_tags` を追加する（TDD）

**Files:**
- Modify: `tests/unit/test_git_repo.py`
- Modify: `backend/repositories/git_repo.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/unit/test_git_repo.py` に追記する：

```python
from backend.repositories.git_repo import iter_tags
from tests.support.git_repo_fixture import make_tagged_repo


def test_iter_tags_軽量タグと注釈付きタグを返す(tmp_path):
    # --- Arrange ---
    repo_path, hash1, hash2 = make_tagged_repo(tmp_path / "repo")
    repo = pygit2.Repository(str(repo_path))

    # --- Act ---
    result = dict(iter_tags(repo))

    # --- Assert ---
    assert result == {"v0.1": hash1, "v1.0": hash2}


def test_iter_tags_タグなしは空を返す(tmp_path):
    # --- Arrange ---
    repo_path = make_two_commit_repo(tmp_path / "repo")
    repo = pygit2.Repository(str(repo_path))

    # --- Act ---
    result = list(iter_tags(repo))

    # --- Assert ---
    assert result == []
```

- [ ] **Step 2: テストを実行して失敗することを確認する**

```bash
uv run pytest tests/unit/test_git_repo.py::test_iter_tags_軽量タグと注釈付きタグを返す -v
```

期待: `ImportError: cannot import name 'iter_tags'`

- [ ] **Step 3: `iter_tags` を実装する**

`backend/repositories/git_repo.py` に追記する（インポートに `Iterator` はすでにある）：

```python
def iter_tags(repo: pygit2.Repository) -> Iterator[tuple[str, str]]:
    """タグ名とそのコミットハッシュを列挙する。

    軽量タグ・注釈付きタグの両方を対象とする。
    コミット以外を指すタグ（blob 等）はスキップする。

    Args:
        repo: 対象リポジトリ。

    Yields:
        ``(タグ名, コミットのフルハッシュ)``。
    """
    for ref_name in repo.references:
        if not ref_name.startswith("refs/tags/"):
            continue
        tag_name = ref_name[len("refs/tags/"):]
        ref = repo.references[ref_name]
        try:
            commit = ref.peel(pygit2.Commit)
        except pygit2.GitError:
            continue
        yield tag_name, str(commit.id)
```

- [ ] **Step 4: テストを実行してパスすることを確認する**

```bash
uv run pytest tests/unit/test_git_repo.py -v
```

期待: 全テスト PASS

- [ ] **Step 5: コミットする**

```bash
git add tests/unit/test_git_repo.py backend/repositories/git_repo.py
git commit -m "feat: iter_tags 関数を追加する"
```

---

### Task 4: Tag CRUD 関数を追加する（TDD）

**Files:**
- Modify: `tests/unit/test_cache_repo.py`
- Modify: `backend/repositories/cache_repo.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/unit/test_cache_repo.py` のインポートに `Tag` を追加する：

```python
from backend.models import Branch, Commit, Repository, Tag
```

`tests/unit/test_cache_repo.py` の末尾に追記する：

```python
# ── タグ CRUD ──────────────────────────────────────────────


def _add_tag(session: Session, repo_id: str, name: str, commit_hash: str) -> None:
    """テスト用タグを挿入する。"""
    cache_repo.insert_tag_row(session, repo_id, name, commit_hash)
    session.commit()


def test_insert_tag_row_正常挿入(session):
    # --- Arrange ---
    _add_repo(session)
    _add_commit(session, "r1", "a" * 40)

    # --- Act ---
    _add_tag(session, "r1", "v1.0", "a" * 40)

    # --- Assert ---
    tags = cache_repo.list_tags(session, "r1")
    assert len(tags) == 1
    assert tags[0].name == "v1.0"
    assert tags[0].commit_hash == "a" * 40


def test_list_tags_複数タグを返す(session):
    # --- Arrange ---
    _add_repo(session)
    _add_commit(session, "r1", "a" * 40)
    _add_tag(session, "r1", "v0.1", "a" * 40)
    _add_tag(session, "r1", "v1.0", "a" * 40)

    # --- Act ---
    result = cache_repo.list_tags(session, "r1")

    # --- Assert ---
    assert {t.name for t in result} == {"v0.1", "v1.0"}


def test_list_tags_タグなしは空を返す(session):
    # --- Arrange ---
    _add_repo(session)

    # --- Act ---
    result = cache_repo.list_tags(session, "r1")

    # --- Assert ---
    assert result == []


def test_get_tags_for_commit_該当タグ名リストを返す(session):
    # --- Arrange ---
    _add_repo(session)
    _add_commit(session, "r1", "a" * 40)
    _add_tag(session, "r1", "v1.0", "a" * 40)
    _add_tag(session, "r1", "v1.0-rc1", "a" * 40)

    # --- Act ---
    result = cache_repo.get_tags_for_commit(session, "r1", "a" * 40)

    # --- Assert ---
    assert set(result) == {"v1.0", "v1.0-rc1"}


def test_get_tags_for_commit_タグなしは空を返す(session):
    # --- Arrange ---
    _add_repo(session)
    _add_commit(session, "r1", "a" * 40)

    # --- Act ---
    result = cache_repo.get_tags_for_commit(session, "r1", "a" * 40)

    # --- Assert ---
    assert result == []


def test_purge_graph_data_タグも削除される(session):
    # --- Arrange ---
    _add_repo(session)
    _add_commit(session, "r1", "a" * 40)
    _add_tag(session, "r1", "v1.0", "a" * 40)

    # --- Act ---
    cache_repo.purge_graph_data(session, "r1")

    # --- Assert ---
    assert cache_repo.list_tags(session, "r1") == []
```

- [ ] **Step 2: テストを実行して失敗することを確認する**

```bash
uv run pytest tests/unit/test_cache_repo.py::test_insert_tag_row_正常挿入 -v
```

期待: `AttributeError: module 'backend.repositories.cache_repo' has no attribute 'insert_tag_row'`

- [ ] **Step 3: Tag CRUD 関数を `cache_repo.py` に実装する**

`backend/repositories/cache_repo.py` に追記する（`from backend.models import ...` の行に `Tag` を追加し、関数を末尾に追記）：

インポート行を以下に変更する：

```python
from backend.models import Branch, Commit, CommitParent, Repository, Tag
```

`purge_graph_data` 関数を以下に置き換える：

```python
def purge_graph_data(session: Session, repo_id: str) -> None:
    """リポジトリのグラフ関連行を全削除する。

    Args:
        session: DB セッション。
        repo_id: リポジトリ ID。
    """
    hashes = [c.hash for c in session.exec(select(Commit).where(Commit.repo_id == repo_id)).all()]
    if hashes:
        session.exec(delete(CommitParent).where(CommitParent.commit_hash.in_(hashes)))  # type: ignore[union-attr,arg-type]  # ty:ignore[unresolved-attribute]
    session.exec(delete(Tag).where(Tag.repo_id == repo_id))  # type: ignore[arg-type]  # ty:ignore[invalid-argument-type]
    session.exec(delete(Branch).where(Branch.repo_id == repo_id))  # type: ignore[arg-type]  # ty:ignore[invalid-argument-type]
    session.exec(delete(Commit).where(Commit.repo_id == repo_id))  # type: ignore[arg-type]  # ty:ignore[invalid-argument-type]
    session.commit()
```

末尾に以下を追記する：

```python
def insert_tag_row(session: Session, repo_id: str, name: str, commit_hash: str) -> None:
    """タグ行を挿入または更新する。呼び出し側で commit する。

    Args:
        session: DB セッション。
        repo_id: リポジトリ ID。
        name: タグ名。
        commit_hash: タグが指すコミットのフルハッシュ。
    """
    session.merge(Tag(name=name, repo_id=repo_id, commit_hash=commit_hash))


def list_tags(session: Session, repo_id: str) -> list[Tag]:
    """リポジトリの全タグを返す。

    Args:
        session: DB セッション。
        repo_id: リポジトリ ID。

    Returns:
        Tag のリスト。
    """
    return list(session.exec(select(Tag).where(Tag.repo_id == repo_id)).all())


def get_tags_for_commit(session: Session, repo_id: str, commit_hash: str) -> list[str]:
    """コミットに付けられたタグ名リストを返す。

    Args:
        session: DB セッション。
        repo_id: リポジトリ ID。
        commit_hash: コミットのフルハッシュ。

    Returns:
        タグ名のリスト。タグなしのときは空リスト。
    """
    rows = session.exec(
        select(Tag).where(Tag.repo_id == repo_id, Tag.commit_hash == commit_hash)
    ).all()
    return [row.name for row in rows]
```

- [ ] **Step 4: テストを実行してパスすることを確認する**

```bash
uv run pytest tests/unit/test_cache_repo.py -v
```

期待: 全テスト PASS

- [ ] **Step 5: コミットする**

```bash
git add tests/unit/test_cache_repo.py backend/repositories/cache_repo.py
git commit -m "feat: Tag CRUD 関数を追加する"
```

---

### Task 5: 同期サービスにタグ同期を追加する（TDD）

**Files:**
- Modify: `tests/unit/test_sync_service.py`
- Modify: `backend/services/sync_service.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/unit/test_sync_service.py` に追記する：

```python
def test_sync_repository_タグが同期される(session, tmp_path):
    # --- Arrange ---
    from tests.support.git_repo_fixture import make_tagged_repo

    repo_path, hash1, hash2 = make_tagged_repo(tmp_path / "repo")
    repo_id = str(uuid.uuid4())
    cache_repo.insert_repository(session, repo_id, str(repo_path), "repo")

    # --- Act ---
    sync_service.sync_repository(session, repo_id, str(repo_path))

    # --- Assert ---
    tags = cache_repo.list_tags(session, repo_id)
    tag_map = {t.name: t.commit_hash for t in tags}
    assert tag_map == {"v0.1": hash1, "v1.0": hash2}


def test_sync_repository_ウォーク範囲外のタグはスキップされる(session, tmp_path):
    # --- Arrange ---
    # タグなしリポジトリで同期後、タグが 0 件であること
    repo_path = make_two_commit_repo(tmp_path / "repo")
    repo_id = str(uuid.uuid4())
    cache_repo.insert_repository(session, repo_id, str(repo_path), "repo")

    # --- Act ---
    sync_service.sync_repository(session, repo_id, str(repo_path))

    # --- Assert ---
    assert cache_repo.list_tags(session, repo_id) == []
```

- [ ] **Step 2: テストを実行して失敗することを確認する**

```bash
uv run pytest tests/unit/test_sync_service.py::test_sync_repository_タグが同期される -v
```

期待: FAIL（タグが 0 件のまま）

- [ ] **Step 3: `sync_service.py` にタグ同期を追加する**

`backend/services/sync_service.py` のインポート行を修正する：

```python
from backend.repositories.git_repo import (
    iter_local_branches,
    iter_tags,
    open_repository,
    walk_commits_from_branches,
)
```

`_sync_commits_and_branches` 関数の `session.commit()` 直前に以下を追加する：

```python
    for tag_name, commit_hash in iter_tags(repo):
        if cache_repo.get_commit(session, repo_id, commit_hash) is not None:
            cache_repo.insert_tag_row(session, repo_id, tag_name, commit_hash)
```

関数全体は以下になる：

```python
def _sync_commits_and_branches(
    session: Session,
    repo_id: str,
    repo: pygit2.Repository,
    head_hex: str,
) -> None:
    """コミット・ブランチ・タグをキャッシュに書き込む。

    Args:
        session: DB セッション。
        repo_id: リポジトリ ID。
        repo: pygit2 リポジトリ。
        head_hex: 現在の HEAD ハッシュ。
    """
    commits = walk_commits_from_branches(repo)
    # 親ハッシュへの外部キー制約を満たすため、コミットを先に全件挿入する。
    for c in commits:
        message_line = c.message.split("\n", 1)[0]
        cid = str(c.id)
        cache_repo.insert_commit_row(
            session,
            repo_id,
            cid,
            cid[:7],
            message_line,
            c.author.name,
            c.author.email,
            int(c.commit_time),
        )
    for c in commits:
        for pos, parent_id in enumerate(c.parent_ids):
            cache_repo.insert_parent_row(session, str(c.id), str(parent_id), pos)
    try:
        for branch_name, tip in iter_local_branches(repo):
            cache_repo.insert_branch_row(session, repo_id, branch_name, tip, 0)
    except pygit2.GitError:
        pass
    for tag_name, commit_hash in iter_tags(repo):
        if cache_repo.get_commit(session, repo_id, commit_hash) is not None:
            cache_repo.insert_tag_row(session, repo_id, tag_name, commit_hash)
    session.commit()
    cache_repo.update_sync_state(session, repo_id, head_hex)
```

- [ ] **Step 4: テストを実行してパスすることを確認する**

```bash
uv run pytest tests/unit/test_sync_service.py -v
```

期待: 全テスト PASS

- [ ] **Step 5: コミットする**

```bash
git add tests/unit/test_sync_service.py backend/services/sync_service.py
git commit -m "feat: 同期サービスにタグ同期を追加する"
```

---

### Task 6: HTML ルーターにタグデータを追加する

**Files:**
- Modify: `backend/routers/html.py`

- [ ] **Step 1: `graph_page` に `tags_by_hash` を追加する**

`backend/routers/html.py` の `graph_page` 関数内、`context = _build_graph_context(...)` の前に以下を追加する：

```python
    tags = cache_repo.list_tags(session, rid)
    tags_by_hash: dict[str, list[str]] = {}
    for tag in tags:
        tags_by_hash.setdefault(tag.commit_hash, []).append(tag.name)
```

`_build_graph_context` の呼び出しを以下に変更する：

```python
    context = _build_graph_context(rid, rec, nodes, edges, branch_lanes)
    context["tags_by_hash"] = tags_by_hash
    context["repos"] = cache_repo.list_repositories(session)
    context["current_repo_id"] = rid
```

- [ ] **Step 2: `commit_detail` にタグリストを追加する**

`commit_detail` 関数を以下に変更する：

```python
@router.get(
    "/repos/{repo_id}/commits/{commit_hash}/detail",
    response_class=HTMLResponse,
)
async def commit_detail(
    request: Request,
    repo_id: str,
    commit_hash: str,
    session: Session = Depends(get_session),
) -> HTMLResponse:
    """コミット詳細の HTML 断片を返す（htmx 用）。"""
    rid = parse_repo_id(repo_id)
    ch = parse_commit_hash(commit_hash)
    row = cache_repo.get_commit(session, rid, ch)
    if row is None:
        raise HTTPException(status_code=404, detail="コミットが見つかりません")
    tags = cache_repo.get_tags_for_commit(session, rid, ch)
    return templates.TemplateResponse(
        request, "partials/detail.html", {"commit": row, "tags": tags}
    )
```

- [ ] **Step 3: テストを実行して既存テストが壊れていないことを確認する**

```bash
uv run pytest tests/unit/ -v
```

期待: 全テスト PASS

- [ ] **Step 4: コミットする**

```bash
git add backend/routers/html.py
git commit -m "feat: グラフ・詳細ルーターにタグデータを追加する"
```

---

### Task 7: テンプレートにタグを表示する

**Files:**
- Modify: `backend/templates/graph.html`
- Modify: `backend/templates/partials/detail.html`

- [ ] **Step 1: `graph.html` のコミット円ループにタグラベルを追加する**

`graph.html` の `{# コミット円 #}` ブロック内、`</g>` タグの直前（`<circle .../>` の後）に以下を追加する：

```xml
        {# コミット円 #}
        {% for node in nodes %}
          {% set color = lane_colors[node.lane % 8] %}
          <g
            class="commit-node"
            data-msg="{{ node.commit.message.split('\n')[0] }}"
            hx-get="/repos/{{ repo_id }}/commits/{{ node.commit.hash }}/detail"
            hx-target="#commit-detail"
            hx-swap="innerHTML"
            _="on mouseenter set #commit-tooltip.textContent to my @data-msg
                             then set #commit-tooltip.style.display to 'block'
                             then set #commit-tooltip.style.top to (event.clientY - 36) + 'px'
                             then set #commit-tooltip.style.left to (event.clientX + 12) + 'px'
               on mouseleave set #commit-tooltip.style.display to 'none'
               on click remove .selected from .commit-node then add .selected to me"
          >
            <circle cx="{{ node.x }}" cy="{{ node.y }}" r="8" fill="{{ color }}"/>
            {% if tags_by_hash.get(node.commit.hash) %}
            <text x="{{ node.x + 14 }}" y="{{ node.y + 4 }}"
                  font-size="10" fill="#e07b00" font-weight="bold"
                  pointer-events="none">
              {{ tags_by_hash[node.commit.hash] | join(", ") }}
            </text>
            {% endif %}
          </g>
        {% endfor %}
```

- [ ] **Step 2: `detail.html` にタグフィールドを追加する**

`backend/templates/partials/detail.html` を以下に変更する：

```html
<div class="commit-detail-body l--stack -g:10">
  <p><strong>ハッシュ</strong> {{ commit.hash }}</p>
  <p><strong>短縮</strong> {{ commit.short_hash }}</p>
  <p><strong>メッセージ</strong> {{ commit.message }}</p>
  <p><strong>作者</strong> {{ commit.author_name }} &lt;{{ commit.author_email }}&gt;</p>
  <p><strong>日時（UNIX）</strong> {{ commit.committed_at }}</p>
  {% if tags %}
  <p><strong>タグ</strong> {{ tags | join(", ") }}</p>
  {% endif %}
</div>
```

- [ ] **Step 3: 全テストを実行して確認する**

```bash
uv run pytest tests/unit/ -v
```

期待: 全テスト PASS

- [ ] **Step 4: コミットする**

```bash
git add backend/templates/graph.html backend/templates/partials/detail.html
git commit -m "feat: グラフと詳細パネルにタグ名を表示する"
```

---

### Task 8: 動作確認

**Files:** なし（手動確認）

- [ ] **Step 1: 開発サーバーを起動する**

```bash
uv run task dev
```

- [ ] **Step 2: ブラウザで確認する**

`http://localhost:8000` にアクセスし、タグが付いたリポジトリを登録してグラフ画面を開く。

確認ポイント：
- タグが付いたコミット円の右にオレンジのタグ名が表示される
- 複数タグはカンマ区切りで表示される
- コミット円をクリックすると詳細パネルにタグ名が表示される
- タグのないコミットにはタグ表示がない

- [ ] **Step 3: 全テストを実行して回帰がないことを確認する**

```bash
uv run pytest tests/unit/ tests/integration/ -v
```

期待: 全テスト PASS
